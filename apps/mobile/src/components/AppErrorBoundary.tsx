import { Component, type ErrorInfo, type PropsWithChildren, type ReactNode } from 'react'; import { StyleSheet, Text, View } from 'react-native'; import { colors, spacing } from '@sprout/design-tokens'; import { analytics } from '../services/analyticsService'; import { AppButton } from './AppButton';
interface State { error: Error | null }
export class AppErrorBoundary extends Component<PropsWithChildren, State> { state: State = { error: null }; static getDerivedStateFromError(error: Error): State { return { error }; } componentDidCatch(error: Error, info: ErrorInfo) { analytics.captureError(error, info.componentStack ?? 'render'); } render(): ReactNode { if (!this.state.error) return this.props.children; return <View style={styles.root}><Text style={styles.icon}>🌧️</Text><Text style={styles.title}>A storm passed through</Text><Text style={styles.copy}>Your garden data is safe. Try returning to the app.</Text><AppButton label="Try again" onPress={() => this.setState({ error: null })} /></View>; } }
const styles = StyleSheet.create({ root: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: spacing.md, padding: spacing.xl, backgroundColor: colors.sand }, icon: { fontSize: 52 }, title: { color: colors.ink, fontSize: 26, fontWeight: '900' }, copy: { color: colors.muted, textAlign: 'center' } });
