import Svg, { G, Line, Path } from 'react-native-svg'; import { computeRadialLeaves, getGrowthState, type PlantProps } from '@sprout/shared'; import { FlawlessAura } from '../shared/FlawlessAura'; import { GroundShadow } from '../shared/GroundShadow'; import { PlantPot } from '../shared/PlantPot'; import { ScarredAccents } from '../shared/ScarredAccents';
export function AlocasiaTinyDancerPlant(props: PlantProps) { const { size = 260 } = props; const growth = getGrowthState(props); const leaves = computeRadialLeaves(growth.growthPercent, growth.asymmetry, { leafCountBase:1, leafDensity:16, maxLeaves:5, angleSpreadDeg:55, lengthBase:55, lengthMultiplier:.95 }); const stem = growth.isWithered ? '#8FA37E' : '#4A7A4E'; const leaf = growth.isWithered ? '#8FA37E' : '#2F6B3A'; const vein = growth.isWithered ? '#B9C6A9' : '#5FA063'; const bladePath = (x:number,y:number,rotation:number,length:number) => { const rad = rotation * Math.PI / 180; const back = { x:x-Math.sin(rad)*length,y:y+Math.cos(rad)*length }; const left={x:(back.x+x)/2-Math.cos(rad)*length*.35,y:(back.y+y)/2-Math.sin(rad)*length*.35}; const right={x:(back.x+x)/2+Math.cos(rad)*length*.35,y:(back.y+y)/2+Math.sin(rad)*length*.35}; return `M${x} ${y} L${left.x} ${left.y} L${back.x} ${back.y} L${right.x} ${right.y} Z`; }; return <Svg width={size} height={size} viewBox="0 0 400 400" accessibilityLabel={`Alocasia Tiny Dancer at ${Math.round(growth.growthPercent)}% growth`}>{growth.isCompleted&&growth.finalVariant==='flawless'?<FlawlessAura color="#7FA35C"/>:null}<GroundShadow/><PlantPot color="#8B6F47" colorLight="#A9835A" colorDark="#6B4A2F"/>{leaves.map((item,index)=><G key={index}><Path d={item.path} stroke={stem} strokeWidth="2.5" fill="none"/><Path d={bladePath(item.tipX,item.tipY,item.rotationDeg,growth.isWithered?20:30)} fill={leaf} opacity={growth.isWithered?.6:1}/><Line x1={item.tipX} y1={item.tipY} x2={item.tipX-Math.sin(item.rotationDeg*Math.PI/180)*(growth.isWithered?16:24)} y2={item.tipY+Math.cos(item.rotationDeg*Math.PI/180)*(growth.isWithered?16:24)} stroke={vein} strokeWidth="1" opacity={.7}/></G>)}{growth.isCompleted&&growth.finalVariant==='scarred'?<ScarredAccents marks={[{cx:179,cy:296,rx:6,ry:4},{cx:222,cy:298,rx:6,ry:4}]}/>:null}</Svg>; }
