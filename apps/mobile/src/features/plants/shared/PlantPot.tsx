import { Ellipse, Path, Rect } from 'react-native-svg';
export function PlantPot({ color, colorLight, colorDark, halfWidth = 50 }: { color: string; colorLight: string; colorDark: string; halfWidth?: number }) { const left = 200 - halfWidth; const right = 200 + halfWidth; return <><Rect x={left} y="300" width={halfWidth * 2} height="16" rx="2" fill={color} /><Path d={`M${left - 10} 300 L${right + 10} 300 L${right - 10} 340 L${left + 10} 340 Z`} fill={colorLight} /><Path d={`M${left - 10} 300 L${right + 10} 300 L${right + 8} 308 L${left - 8} 308 Z`} fill={color} /><Ellipse cx="200" cy="300" rx={halfWidth - 5} ry="7" fill={colorDark} opacity={.4} /></>; }
