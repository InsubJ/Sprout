import type { Friendship, WitherNudge } from '@sprout/shared'; import type { SupabaseClient } from '@supabase/supabase-js'; import type { SocialRepository } from '../repositories/socialRepository';
export class SupabaseSocialRepository implements SocialRepository { constructor(private readonly client: SupabaseClient) { if (!client) throw new Error('Supabase client is required'); }
  async getFriendships(userId: string) { if (!userId.trim()) throw new Error('User ID is required'); const { data, error } = await this.client.from('friendships').select('*').or(`user_id.eq.${userId},friend_id.eq.${userId}`); if (error) throw new Error(`Unable to load friends: ${error.message}`); return (data ?? []) as Friendship[]; }
  async sendFriendRequest(userId: string, friendId: string) { if (!userId.trim() || !friendId.trim() || userId === friendId) throw new Error('Choose another gardener'); const { data, error } = await this.client.from('friendships').insert({ user_id: userId, friend_id: friendId, status: 'pending' }).select().single(); if (error) throw new Error(`Unable to send request: ${error.message}`); return data as Friendship; }
  async respond(friendshipId: string, status: 'accepted' | 'declined') { if (!friendshipId.trim()) throw new Error('Friendship ID is required'); const { data, error } = await this.client.from('friendships').update({ status }).eq('id', friendshipId).select().single(); if (error) throw new Error(`Unable to respond: ${error.message}`); return data as Friendship; }
  async getNudgedHabitIds(senderId: string, receiverId: string, date: string): Promise<string[]> { if (!senderId.trim() || !receiverId.trim() || !/^\d{4}-\d{2}-\d{2}$/.test(date)) throw new Error('Valid sender, receiver and date are required'); const { data, error } = await this.client.from('wither_nudges').select('habit_id').eq('sender_id', senderId).eq('receiver_id', receiverId).eq('nudged_at', date); if (error) throw new Error(`Unable to load nudges: ${error.message}`); return (data ?? []).map((row) => row.habit_id as string); }
  async sendNudge(senderId: string, receiverId: string, habitId: string) { if (!senderId.trim() || !receiverId.trim() || !habitId.trim() || senderId === receiverId) throw new Error('Valid sender, receiver and habit IDs are required'); const { data, error } = await this.client.from('wither_nudges').insert({ sender_id: senderId, receiver_id: receiverId, habit_id: habitId }).select().single(); if (error) throw new Error(`Unable to send nudge: ${error.message}`); return data as WitherNudge; }
}
