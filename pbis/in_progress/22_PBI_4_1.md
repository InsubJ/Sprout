# Product Backlog Item (PBI) 4.1: Wither State Detection & Nudge Schema

## Description
Implement backend rules to transition plants into withered state and record user nudges.

## User Constraints & Rules
- Follow all core principles defined in [AGENTS.md](file:///c:/Users/ijeon/Documents/Sprout/.agents/AGENTS.md) (SRP, SOLID, DbC, DRY/YAGNI).
- Strictly write Vanilla CSS / CSS Modules only (No TailwindCSS).
- Define contracts for functions and inputs using TypeScript.

## Requirements
- SQL migration creating wither_nudges table.
- Configure logic/database rules to auto-transition active habits to withered when missed frequency limit exceeds threshold.
- Enforce unique constraint preventing duplicate daily nudges: UNIQUE (sender_id, habit_id, nudged_at).

## Verification & Testing
- **Test File**: \$testFile\
- **Action**: Create database trigger checks verifying 24h nudge throttling in ackend/supabase/tests/nudge_rules.test.sql.

## Kanban State & Hand-off Instructions
When you start this task:
1. Ensure this file is located in the \pbis/in_progress/\ directory.

When this PBI is fully implemented, verified, and passing tests:
1. Open the [task.md](file:///C:/Users/ijeon/.gemini/antigravity-ide/brain/ec665ca2-f627-4463-a665-9be00ba9b8a3/task.md) backlog tracking file and check this item off.
2. Move this PBI file (\$(@{Id=4.1; Index=22; NextId=4.2; NextFile=23_PBI_4_2.md; Title=Wither State Detection & Nudge Schema; Description=Implement backend rules to transition plants into withered state and record user nudges.; Requirements=System.Object[]; Test=Create database trigger checks verifying 24h nudge throttling in ackend/supabase/tests/nudge_rules.test.sql.}.Index)_PBI_4_1.md\) from \pbis/in_progress/\ to \pbis/completed/\.
3. If this is not the final PBI, locate the next PBI file (\$(@{Id=4.1; Index=22; NextId=4.2; NextFile=23_PBI_4_2.md; Title=Wither State Detection & Nudge Schema; Description=Implement backend rules to transition plants into withered state and record user nudges.; Requirements=System.Object[]; Test=Create database trigger checks verifying 24h nudge throttling in ackend/supabase/tests/nudge_rules.test.sql.}.NextFile)\) inside the \pbis/backlog/\ directory, move it to \pbis/in_progress/\ directory, and mark it as in-progress in \	ask.md\.
