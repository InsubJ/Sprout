# Product Backlog Item (PBI) 4.6: SVG Plant Asset Integration (PlantRenderer.tsx)

## Description
Integrate the already created SVG plant components under `@directory:components/plants` into the habit details and card rendering flow.

## User Constraints & Rules
- Follow all core principles defined in [AGENTS.md](file:///c:/Users/ijeon/Documents/Sprout/.agents/AGENTS.md) (SRP, SOLID, DbC, DRY/YAGNI).
- Strictly write Vanilla CSS / CSS Modules only (No TailwindCSS).
- Define contracts for functions and inputs using TypeScript.

## Requirements
- Create component `PlantRenderer.tsx` in `frontend/src/components/habit/` (or repurpose `Plant.tsx` from `frontend/src/components/plants/`).
- Use the existing SVG components under `frontend/src/components/plants/` (`BonsaiPlant`, `DesertCactusPlant`, etc.) rather than building a new SVG engine.
- Integrate this `PlantRenderer` into `HabitCard.tsx` so that the plant SVG is rendered dynamically based on habit properties (`plant_type`, `current_waterings`, `target_waterings`, `wither_count`, `status`).

## Verification & Testing
- **Test File**: $testFile$
- **Action**: Create rendering snapshot and integration tests in `frontend/src/tests/PlantRenderer.test.tsx` verifying correct SVG rendering of different plant species and growth percentages.

## Kanban State & Hand-off Instructions
When you start this task:
1. Ensure this file is located in the \pbis/in_progress/\ directory.

When this PBI is fully implemented, verified, and passing tests:
1. Open the [task.md](file:///C:/Users/ijeon/.gemini/antigravity-ide/brain/8a7f53f5-bd80-4759-9064-45f01c8ea3e3/task.md) backlog tracking file and check this item off.
2. Move this PBI file ($( @{Id=4.6; Index=27; NextId=4.7; NextFile=28_PBI_4_7.md; Title=SVG Plant Asset Integration (PlantRenderer.tsx); Description=Integrate existing plant SVG components into HabitCard using a PlantRenderer wrapper.; Requirements=System.Object[]; Test=Create rendering snapshot tests in frontend/src/tests/PlantRenderer.test.tsx verifying correct plant rendering.}.Index )_PBI_4_6.md) from \pbis/in_progress/\ to \pbis/completed/\.
3. If this is not the final PBI, locate the next PBI file ($( @{Id=4.6; Index=27; NextId=4.7; NextFile=28_PBI_4_7.md; Title=SVG Plant Asset Integration (PlantRenderer.tsx); Description=Integrate existing plant SVG components into HabitCard using a PlantRenderer wrapper.; Requirements=System.Object[]; Test=Create rendering snapshot tests in frontend/src/tests/PlantRenderer.test.tsx verifying correct plant rendering.}.NextFile )) inside the \pbis/backlog/\ directory, move it to \pbis/in_progress/\ directory, and mark it as in-progress in \task.md\.
