# Product Backlog Item (PBI) 4.7: Plant Species Difficulty & Frequency Alignment

## Description
Align the difficulty calculation and species selection logic in `frontend/src/utils/difficulty.ts` with the actual 9 plant species and define their frequency metrics.

## User Constraints & Rules
- Follow all core principles defined in [AGENTS.md](file:///c:/Users/ijeon/Documents/Sprout/.agents/AGENTS.md) (SRP, SOLID, DbC, DRY/YAGNI).
- Strictly write Vanilla CSS / CSS Modules only (No TailwindCSS).
- Define contracts for functions and inputs using TypeScript.

## Requirements
- Map the 9 plant species (`pothos`, `spider_plant`, `bonsai`, `lavender`, `sunflower`, `midnight_rose`, `desert_cactus`, `golden_oak`, `ethereal_sakura`) to their respective difficulty tiers in `TIER_SPECIES` inside `difficulty.ts`.
- Assign a natural or recommended frequency metric/association to each of the 9 plant species based on their tier/characteristics (e.g. mapping species to their natural/recommended frequencies).
- Update the species assignment logic (`assignSpecies` and related functions) to select deterministically/correctly from these 9 species.
- Update/add unit tests in `frontend/src/tests/difficulty.test.ts` to verify the updated species list and frequency mappings.

## Verification & Testing
- **Test File**: $testFile$
- **Action**: Run the difficulty unit tests `vitest run frontend/src/tests/difficulty.test.ts` ensuring all test cases match the new species map and pass.

## Kanban State & Hand-off Instructions
When you start this task:
1. Ensure this file is located in the \pbis/in_progress/\ directory.

When this PBI is fully implemented, verified, and passing tests:
1. Open the [task.md](file:///C:/Users/ijeon/.gemini/antigravity-ide/brain/8a7f53f5-bd80-4759-9064-45f01c8ea3e3/task.md) backlog tracking file and check this item off.
2. Move this PBI file ($( @{Id=4.7; Index=28; NextId=End; NextFile=None; Title=Plant Species Difficulty & Frequency Alignment; Description=Align the difficulty calculation and species selection logic in difficulty.ts with the actual 9 plant species and define their frequency metrics.; Requirements=System.Object[]; Test=Run the difficulty unit tests vitest run frontend/src/tests/difficulty.test.ts.}.Index )_PBI_4_7.md) from \pbis/in_progress/\ to \pbis/completed/\.
3. If this is not the final PBI, locate the next PBI file ($( @{Id=4.7; Index=28; NextId=End; NextFile=None; Title=Plant Species Difficulty & Frequency Alignment; Description=Align the difficulty calculation and species selection logic in difficulty.ts with the actual 9 plant species and define their frequency metrics.; Requirements=System.Object[]; Test=Run the difficulty unit tests vitest run frontend/src/tests/difficulty.test.ts.}.NextFile )) inside the \pbis/backlog/\ directory, move it to \pbis/in_progress/\ directory, and mark it as in-progress in \task.md\.
